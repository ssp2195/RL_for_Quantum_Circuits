# Article V1 ten-minute claim traceability

| Claim/evidence | Authoritative artifact or gate |
|---|---|
| Action is a persistent frontier record | Existing search trace parity tests |
| Expansion grammar and exact semantics unchanged | Native grammar, symbolic, canonicalization, archive, and certification regression suites |
| Runtime cap selected without test access | `calibration/calibration.json` and `no_test_access=true` |
| One physical run feeds threshold rows | `raw_runs.jsonl` plus derived anytime table and count audit |
| Process CPU is the operability metric | V3 raw `process_cpu_time_ns/process_cpu_seconds` |
| Timeouts are incomplete, not failures | V3 campaign audit |
| Primary learner excludes hard targets | V5 checkpoint eligible difficulties and ordered target IDs |
| Equal-CPU results are supplementary | `cpu_budget_secondary/` and separate schema |
| Returned witnesses remain exact | Fresh DAG reconstruction and Article V1 certifier diagnostics |

No scientific claim is authorized until the frozen config, raw-ledger audit,
and source/config digests agree.
